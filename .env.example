# Optional — only needed if you want to use the in-app chat panel.
# The setup wizard can write this for you, or you can paste it here directly.
# Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional — model used by the chat panel. Defaults to gpt-4o-mini.
# OPENAI_MODEL=gpt-4o-mini
